# 项目协作总则
## 开发模式
本项目采用 **SDD（规格驱动开发）+ TDD（测试驱动开发）** 模式，所有功能开发必须先定规格、再写测试、最后实现。

## 规则优先级（从高到低）
1. 本 CLAUDE.md 定义的项目规则
2. 项目内 spec.md / tasks.md / checklist.md 定义的规格与验收标准
3. Superpowers 技能库的默认执行逻辑
4. AI 系统默认行为

## 强制产出物
所有需求落地必须输出三份文档，保存到项目根目录：
- spec--xx模块.md：需求规格，定死「做什么、不做什么、什么算完成」
- tasks--xx模块.md：任务拆分，定死「开发顺序、每个任务的边界」
- checklist--xx模块.md：验收标准，所有项必须可执行、可验证
- 文档写道docs下的spec目录和tasks目录和checklist目录


# TDD 开发铁律
1. 测试先行：任何功能代码编写前，必须先编写对应的单元测试用例，确认测试运行失败（Red阶段）
2. 最小实现：只编写刚好让测试通过的代码，不提前实现未定义的功能（Green阶段）
3. 持续重构：测试通过后，在保持测试全绿的前提下优化代码结构（Refactor阶段）
4. 禁止补测：不允许先写功能代码，后补测试用例
5. 测试挂起=开发失败：任何代码变更导致现有测试失败，必须立即修复，不能进入下一个任务

# 强制使用的 Superpowers 技能
1. 所有新需求从 brainstorming 开始，设计未批准禁止写代码
2. 所有开发任务必须使用 test-driven-development 技能，严格执行TDD循环
3. 所有任务完成后必须使用 verification-before-completion 验证，禁止无证据声称完成
4. 多模块并行开发使用 subagent-driven-development，确保每个子任务符合规格
