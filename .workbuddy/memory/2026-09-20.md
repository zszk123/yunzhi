# 2026-09-20 工作日志

## Pixso AI 插件安装（18:12-18:42）

1. **MCP 连接验证**：`~/.workbuddy/mcp.json` 已有 Pixso MCP（http://127.0.0.1:3667/mcp）。三重验证通过：端口 initialize 握手 HTTP 200 → 客户端实调 `fetch_context` 成功 → 返回真实设计稿上下文（选中节点 2:27、23 个变量、Inter 字体）。
2. **Skills 安装**：clone 官方仓库 `PixsoLtd/pixso-ai-integration`（浅克隆）到工作区 `pixso-ai-integration/`，安全审计通过（P2：5 个 SKILL.md 均为工作流指导，2 个脚本仅与本地 3667 端口通信，Grep 全仓无危险模式），将 5 个 skill 复制到用户级 `C:\Users\吴蒙蒙\.workbuddy\skills\`：
   - pixso-read-dsl / pixso-design-to-code / pixso-code-to-design / pixso-design-editing / pixso-component-config
3. **教训**：
   - 此沙箱 Git Bash 极度精简：`ls`、`tail`、`dirname` 均缺失；复制文件优先用 Node 脚本（node.exe 在 `.workbuddy\binaries\node\versions\22.22.2-3\`）。
   - Glob 的 `pixso-*/**` 模式未命中但 `**/*` 有结果——判定文件缺失前先用 Read 直接验证，避免误判。

## Pixso 文档管理列表页设计（18:42-18:55）

- 按 pixso-design-editing skill 全流程执行：需求规划门（Clear+Medium，免提问）→ 风格灵感 → web-app 指南 → DSL 预检（dsl-rules.md + dsl-schema.ts 全文）→ 3 块 apply_design + 1 次修复 → check_layout 零问题。
- 产物：知识库「文档管理」列表页（节点 7:1，1200×968，位于概览页 2:159 右侧 x=1500），含标题行、检索筛选栏、批量操作栏（选中态）、8 行文档表格、分页。全部颜色/圆角用文件变量 `$Color/*`、`$Radius/*`，无硬编码。
- **关键教训：Pixso apply_design 校验器对中间容器的 `fit_content` 尺寸报 invalid size**——容器尺寸用显式数字或 `fill_container`（fill×fill 组合安全）。文本节点不设尺寸 = auto growth 不报错；固定列宽用 `textGrowth:"fixed-width"` + 数字 width。
- take_screenshot 整页结果 96K 字符超 token 限制无法人工复核（结构校验已过）；链接格式 `{origin}/app/design/{fileKey}?icon_type=1&page-id={pageId}&item-id={guid}`，位置信息用 eval_script 读 pixso.origin/fileKey/currentPage.id。

## 画板关系梳理（19:13）

- 用户文件"页面 1"上共 6 个画板。左列 4 个是过程草稿：2:73 Body（概览完整页=侧边栏+MainContent，即本轮编辑的知识库概览）、3:1 Body（仅侧边栏骨架）、2:27 知识库文档管理（仅 TopBar 骨架）、3:46 Content（文档管理 v1，旧数据"128 个文档"，含 UploadCard）。右列 2 个是当前版：2:465 文档列表与接入（TopBar+Body 完整页）、7:1 DocManagePage（本轮 AI 生成，1200 纯内容区）。
- 演进关系：2:27 的顶栏 + 3:1 的侧边栏思路在 2:465 合体成型；3:46 是文档管理的早期内容版，由 7:1 接替。7:1 与 2:73 的 MainContent 同规格（1200×968），可嵌入 2:465 的 Body。
- 注意：pixso.document.children 不存在，用 pixso.root.children 遍历页面；查询节点父级用 node.parent.id/name。

## 画布重排（19:29）

- 用户拖乱了 4 个草稿的坐标（x -686~-1034 不齐），7:1 内部结构完好未受损。
- 重排为两列分区：左列草稿（2:73@0,0 → 3:1@0,1088 → 2:27@0,2176 → 3:46@0,3320，垂直间距 120），右列当前版（2:465@1560,0、7:1@3100,0）。新增两个分区标签 text：7:340（草稿区）、7:341（当前版区，Primary 色）。
- 移动顶级画板用 eval_script 直接赋值 node.x/node.y，比 apply_design M()（改层级）更合适。

## 产品结构重组（19:39）

- 用户反馈"侧边栏和顶栏应该只有一个，内容一样很混乱"→ 根因：框架重复 4 次 + 文档列表内容重复 2 份（2:465 内置 ListColumn 与 7:1）。
- 重组为「一套框架 × 两张完整页」：clone 2:465 → 页面 A「页面-知识库概览」(7:342)@(0,0)，装入 2:159 概览内容，侧边栏高亮切到"知识库概览"；2:465 改名「页面-文档列表」@(0,1124)，移出旧 ListColumn（2:589，归档不删）、装入 7:1。
- 侧边栏选中态规则：NavItem 背景 PrimarySoft #EFF6FF + 文字 Primary #2563EB；未选中无背景 + 文字 #52525B。切换用 node.fills 赋值。
- 归档区 y=2400 起纵排：2:73 空壳/3:1/2:27/3:46/2:589，间距 120。
- **坑：中途 Pixso 端报 "please open a design file"——用户切走了文件视图，MCP 连接在但无活动画布**，需用户重新打开文件后继续；两个分区标签（7:340/7:341）文字更新未完成，遗留。
- 用户返回后连接恢复，标签更新完成：7:341 改为「完整页 ×2（页面-知识库概览 / 页面-文档列表）」。

## 冗余节点清理（19:54）

- 用户确认 Content/ListColumn 多余、侧边栏双 Body 之一多余，授权"把多余的都删掉"。
- eval_script 删除 6 个节点，脚本带防呆校验（删除前核对 `n.name === t.expect`，不匹配则跳过）：2:73 概览空壳、3:1 侧边栏骨架、2:27 顶栏骨架、3:46 文档管理v1、2:589 旧文档列表内容、7:340 归档区标签。全部命中删除成功。
- 最终画布：`7:342` 页面-知识库概览 @(0,0)、`2:465` 页面-文档列表 @(0,1124)（内含 7:1 内容区）、`7:341` 标签「完整页 ×2」@(0,-48)。结构收敛为「一套框架 × 两张完整页」，无冗余。

## 补齐剩余页面（20:00-20:30）

- 用户要求补齐侧边栏剩余 2 个导航页（检索调试、数据源接入）+ 登录页，共 3 张。
- 流程：eval_script 克隆 2:465 ×2（clone 后按名 findOne 清空 ContentWrap 子项）→ apply_design 分别创建内容 → eval_script 统一切换侧边栏高亮。
- 产物：`10:1` 页面-检索调试（左参数面板 360：查询框/策略 chips/Top-K/开关/历史记录，右结果面板 5 条带得分徽章）、`10:464` 页面-数据源接入（4 统计卡 + 3×2 数据源卡片网格 + PrimarySoft 提示条，异常卡用 Danger 描边）、`10:1127` 页面-登录（1440×1024 居中卡片 420×574：Logo/邮箱/密码/SSO/协议，drop shadow）。
- 画布布局：登录 @0,0；概览 @0,1144；文档列表 @1560,1144；检索调试 @0,2268；数据源接入 @1560,2268（行距 1144、列距 1560）。标签 7:341 已不存在，新建 10:1172「完整页 ×5（…）」@(0,-57)。
- **关键教训：apply_design 新版校验器要求每个容器显式 width/height（fit_content/省略均报 invalid size）**——I() 时直接写数字最省事（数据源页/登录页一次过，检索调试页因省略补了 24 个 U()）。
- **教训 2：ContentWrap 是 padding 24 的 HORIZONTAL autoLayout，子项 x=24,y=24 是正常表现**（7:1 同样形态），勿当成 bug 修；直接赋 x/y 无效（布局引擎控制）。
- 文件变量全表已入档：Color/Background·Surface·SurfaceMuted·Muted·Border·TextPrimary·TextSecondary·TextMuted·Primary·PrimarySoft·PrimarySoftBorder·OnPrimary·Success·SuccessSoft·Danger·DangerSoft·Violet·VioletSoft + Radius/Default·Small·Pill + Size/Sidebar·TopBar。
- 链接数据：origin https://pixso.cn, fileKey BE6geK8QYPj3TqbLx2x5ew, pageId 0:1；check_layout(problemsOnly) 两新页均 null。

## LangGraph Agent 系统模块（20:25-20:55）

- 用户要求在 RAG 知识库系统外新增 Agent 系统（LangGraph 流 + 对话）。产出 2 张页：`10:1217` 页面-Agent 对话（会话列表 240 + 对话区 584（欢迎/用户气泡/2 张工具调用卡/AI 回答带引用 chips/建议 chips/输入栏）+ 运行轨迹面板 280（6 步节点耗时、tokens 统计、上下文））、`10:1691` 页面-Agent 工作流（4 统计卡 + FlowCanvas 1136×680 绝对定位流程图 + TipBar）。
- 流程图：8 节点 S 形两行布局（START 用户消息 → 意图识别路由 → 查询改写 → 混合检索 → ↓ 结果重排 → 答案生成 → 反思评估 → END 输出答案），普通边实线 fill Border，条件边虚线 stroke dashPattern[5,4] Primary（反思→改写回环"≤3 次"、意图→END 闲聊分流），图例 + 说明条。
- **侧边栏全站扩展**：4 张旧页 NavGroup 末尾插入克隆的「Agent 系统」NavLabel + apply_design 建 2 个 NavItem（216×34, icon_font message-circle/git-branch 15×15 + text 13），先插标签再克隆页面使新页自动带 6 项导航。画布第四行 y=3392 两列；标签 10:1172 更新为「完整页 ×7（…）」。
- **坑：setNav 时把旧 NavItem 的矢量图标 frame（15×15）误设 SOLID fills 变成色块**——旧图标是 FRAME 内含矢量路径，frame 本身应 fills=[]；icon_font 创建后也是 FRAME 但字形色在其自身属性。修复：遍历两页 6 项 icon frame 全部恢复 fills=[]（12 处）。教训：给 NavItem 上色只动 TEXT 子节点；icon frame 勿动。
- 两张 Agent 页 check_layout(problemsOnly) 均 null。链接：对话 10:1217、工作流 10:1691。

## Agent 创建入口页（20:50）

- 用户要求：登录后先进入无侧边栏的创建页，页面有「+」方框选择创建 Agent 对话 / Agent 会话流。
- 产物：`10:2407` 页面-Agent 创建 @画布(1560,0)（登录页右侧，产品流：登录 → 创建 → 工作台）。1440×1024，顶栏（品牌 + 铃铛 + G() 生成的人像头像 32 圆）+ Hero 居中（"创建一个新 Agent" 26/700 + 两张 400×280 创建卡：PlusBox 72×72 PrimarySoft 内 plus 28 → "Agent 对话"/"Agent 会话流" + 描述 + "立即创建 →" CTA，卡片带 drop shadow）。
- 标签 10:1172 → 「完整页 ×8（登录 → Agent 创建 / …）」。check_layout null。G() 头像：frame 32×32 cornerRadius 16 + G(node,"stock","professional woman portrait, headshot") 一次成功。

## Agent 工作台全屏化改造（20:58）

- 用户要求：Agent 对话页和 Agent 工作流页去掉左侧边栏；Agent 工作流页加右侧边栏（节点选择 + 节点内部逻辑设计）。
- eval_script 结构改造：删两页 Sidebar（Body fill_container 自动补位 1440）；对话页 AgentChatPage 1200→1440 全链扩宽（MainRow 1376、ChatCol 824、ChatArea/工具卡/AI 卡 +240、文本 fixed-width 同步 716/756/680）；工作流页 FlowPage 1440、TitleRow/Stats(卡 269→329)/TipBar →1376、新建 MainRow 10:2451（1376×764，createFrame + insertChild 到 TipBar 前）装入 canvas(1036×764)。
- apply_design 建 NodePanel 10:2452（320×764）：节点库 Tab + 6 节点项（LLM/工具/检索/条件路由/记忆/输出，iconBox 28 三色）+ 节点逻辑区（IN/OUT 变量徽章 + 5 行 Python 伪代码块 + Top-K/阈值/超时参数徽章 + 保存按钮）。
- **教训：全屏化改造 = resize 链式传播**，fixed-width 文本要逐个 resize(w, h_current)（高度自动重算，读回 15/42 正常）；MainRow/panel 用 createFrame 后 layoutMode/itemSpacing 直接设 Figma API 属性可行。
- 两页 check_layout(problemsOnly) 均 null。Agent 对话/工作流从此无左侧导航（工作台形态），知识库 4 页保持侧边栏导航。

## Agent 创建页重设计（21:05）

- 用户反馈两张创建卡"太廉价"，要求：一个「+」框入口 + 点击弹选择 + 页面保留所有 Agent。
- 重设计（10:2407 内）：删 hero，新建 Content（fill×fill vertical 居中）：Header（"我的 Agent" 22/700 + 统计）+ 2 行网格（3×445×170）：首格虚线新建卡（stroke dashPattern[6,5] + PlusBox 56 PrimarySoft + "新建 Agent"/"对话 / 会话流"）+ 5 张 Agent 卡（iconBox 40 类型三色 + 名称/类型行 + 描述 + 类型徽章/时间 meta + ellipsis）。TypePopover 300×154 绝对定位 @(508,430)（layoutPositioning:true + left/top，page autoLayout 子项浮动）：标题 + 两个选项行（Agent 对话/会话流 + chevron）。
- 坑1：选项行内容 256 > 248 可用宽溢出 8px → col 190→182。坑2：lucide arrow-right 内部矢量 h:0 被 check_layout 标记 → 换 chevron-right 后 null。**教训：check_layout 对 h:0/w:0 的 icon_font 内部矢量会报问题路径；水平线类图标（arrow-right 等）避开，用 chevron-* 替代。**
- 最终 check_layout null。layoutPositioning 浮层模式验证可行。

## 技术选型咨询（21:22）

- 用户为真实项目开发征询选型：语言/框架/Spring Cloud/向量库/Skill+MCP。给出推荐：Java(Spring Boot 管理域) + Python(FastAPI+LangGraph AI 域) 双服务；LangGraph 骨架 + OpenAI 兼容统一接入 + Anthropic 独立 Provider（MewCode 适配层经验复用）；模块化单体不上 Spring Cloud；向量库 Qdrant（原生 sparse+dense+RRF 混合检索匹配 BM25+向量设计）；知识库能力 MCP 工具化、Skill=提示词+工具组合包由 Java 注册；数据层 PG+Qdrant+Redis+MinIO；三阶段落地（Python 单体 MVP → 抽 Java 管理服务 → MCP/Skill）。SVG 架构图已出。

## 技术选型文档落盘（21:40）

- 用户追问"MySQL 还是 PostgreSQL"（已答：AI 生态位差异——JSONB/PostgresSaver/pgvector/中文检索四点 PG 胜），随后要求"写一个技术选型文档，并把上面生成的图填上"。
- 产出 3 文件（工作区根目录）：`技术架构图.svg`（1200×980 分层架构图，9-ramp 浅色规范：蓝客户端/青 Java 域/紫 AI 域/绿数据/橙模型/红 MCP，含图例+落地路线底注）、`技术选型文档.md`（11 章+2 附录：结论总览/架构/语言/框架/单体/向量库/PG vs MySQL/Skill-MCP/中间件/三阶段路线/风险/面试话术/简历衔接，MD 内以相对路径引用 SVG）、`技术选型文档.html`（单文件自包含预览版，SVG 内嵌+完整排版，可直接浏览器打开/打印）。
- 注意：MD 引用本地 SVG 在部分查看器不渲染，HTML 版是"图文完整"的保底形态。

## 补充选型：文档解析 + 向量模型（21:55）

- 用户追问"文档解析用 LlamaIndex 还是其他？向量模型用哪个？还是都有"。结论并入文档：**不整体引入 LlamaIndex**（与 LangGraph 编排职责重叠，只按需借 readers 包）；解析用分层路由（PyMuPDF/pdfplumber/python-docx/pandas 覆盖常规格式 + **MinerU** 本地解析扫描/复杂版面 PDF + LlamaParse 云端可选增强）；向量模型 **BGE-M3 主力**（单模型 dense+sparse 双输出 → 直接喂 Qdrant sparse+dense+RRF，连 ES 的 BM25 都省了；8192 上下文适配法律长文档）+ bge-reranker-v2-m3 配套，Qwen3-Embedding 作升级备选。
- 关键澄清已写入文档：同一 Qdrant collection 必须同一 embedding 模型，混用即向量空间不兼容；"都用"的正确姿势是分 collection 或评测后整体切换。
- MD/HTML 两版同步更新：总览表 +2 行，新增第 9 章（文档解析）、第 10 章（向量模型），原 9/10/11 章顺延为 11/12/13，目录与锚点同步改。

## 数据库设计（22:03）

- 用户要求设计 SQL 数据库。产出 `schema.sql`（PostgreSQL 17 可执行 DDL，15 表全中文注释）+ show_widget ER 关系图（核心 11 表，680 宽规范，JSONB 字段琥珀斜体标注）。
- 15 表 5 模块：用户权限（users/roles/user_roles）、知识库域（knowledge_bases/documents/document_chunks/data_sources/parse_tasks/retrieval_logs）、Skill 与 Agent（skills/agents/agent_skills/agent_kbs）、会话与运行（agent_sessions/agent_messages/workflows/workflow_runs）、审计（audit_logs）。
- 关键设计决策已向用户讲解：①KB 表记 embedding_model（换模型=重嵌入）且 1 KB=1 Qdrant collection；②chunks 表 Parent-Child 自引用 + qdrant_point_id（向量在 Qdrant、正文在 PG，payload 只放引用防双写不一致）；③agent_sessions.id 用 UUID 直接当 LangGraph PostgresSaver thread_id（checkpoint 表由其自动创建）；④workflows.graph 整体存 JSONB 不拆 nodes/edges（整体读写+节点类型演进）；⑤桥接表用 agent_skills/agent_kbs 而非数组；⑥阶段②拆服务用 PG schema（admin/ai）迁移。
- 所有表都能对应设计稿页面（parse_status↔文档列表状态列、citations↔引用 chips、node_traces↔TracePanel、retrieval_logs↔检索调试历史）。

## 开发计划文档（22:25）

- 用户要求生成开发计划文档。产出 `开发计划.md`：三阶段 × 七迭代（阶段① Python 单体 MVP：骨架/Ingestion/检索对话；阶段② Java 管理服务：管理域/组网；阶段③ MCP 与交付：MCP+工作流/评测交付），每任务带产出物+可勾选验收标准；M1/M2/M3 三个 e2e 里程碑；在职业余节奏 18 周（每周 10-12h，弹性 ±30%）。
- 特色设计：第 5 节"降级顺序"（砍范围不砍里程碑：MinerU→PyMuPDF、拖拽→表单、评测缩指标、流式→同步）；第 7 节"简历素材沉淀"把每迭代映射到简历叙事（法律 RAG 讲演进、MewCode 讲一致性、在职经验讲协作）。
- show_widget 出了甘特式迭代时间线（三行阶段条 + M1/M2/M3 旗标）。至此项目文档链完整：技术选型 → schema.sql → 开发计划，下一步可直接启动迭代 1。
