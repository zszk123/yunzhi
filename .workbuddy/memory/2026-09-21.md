# 2026-09-21 工作日志

## 迭代 1 启动决策（15:03-15:16）

- 用户问"开发计划写好后是直接写代码还是写 PRD"。结论：**不写 PRD**（开发计划+技术选型+schema+Pixso 设计稿已覆盖 PRD 职责），按 CLAUDE.md 走 SDD：每个迭代开工前产出 spec/tasks/checklist 三件套，按模块拆而非一次写全系统。
- 概念讲解（用户偏好 Java 锚点）：PG17=账本（15 表结构化数据）、MinIO=文件柜（文件本体，S3 协议兼容以后可平移 OSS）；SQLAlchemy≈JPA @Entity、Alembic≈Flyway；pydantic-settings≈application.yml+@ConfigurationProperties；structlog JSON≈logback+logstash-encoder。

## 迭代 1 brainstorming（15:25-15:32）

- 按 brainstorming skill 流程：先读技术选型文档+schema.sql 排除已定决策，只问 3 个悬而未决点。确认：工作区 `E:\wmm\yunzhi` 尚非 git 仓库（T1 要 init）。
- **决策结果**：① Monorepo 单仓（backend/ + frontend/ + docs/，迭代 4 加 admin/）② 前端 Vite+React+**TS+AntD**（理由：8 页管理台表格表单多、前端是配角不在配角上花主角时间、TS 对齐 15 表结构）③ Python **3.12**（AI 生态 wheel 最成熟，3.13 易源码编译卡壳）。

## 迭代 1 三件套产出（15:32）

- 产出 `docs/spec/spec--项目骨架与基础设施.md`、`docs/tasks/tasks--项目骨架与基础设施.md`、`docs/checklist/checklist--项目骨架与基础设施.md`（遵循 CLAUDE.md 目录规范）。
- spec 定死的关键约定：docker-compose 只管四件套（应用本地 uvicorn/pnpm dev 跑，应用容器化留迭代 5）；PG 镜像用 `pgvector/pgvector:pg17`；MinIO bucket `yunzhi-files`（初始化容器自动建）；DB 名/用户 yunzhi/yunzhi；Alembic 只管 15 表（checkpoints 表迭代 3 由 PostgresSaver 自建）；端口表（5432/6333/6379/9000/9001/8000/5173）；structlog JSON 日志；pytest 加 `--basetemp=.pytest_tmp`（用户环境沙箱会拦 pytest 删 TEMP）。
- tasks 13 个任务（T1-T13），末尾 T12 接入主流程 + T13 端到端验证（含干净目录模拟新机器 e2e ≤30min、.env.test 切环境零改码 e2e-2）。
- 状态：**待用户评审 spec**，批准后进入 TDD 实现（CLAUDE.md 铁律：先测试后实现）。

## 迭代 1 实现（15:49-16:30，TDD 执行）

- 开工时发现 T1-T5 已由更早会话完成（git init/.gitignore/pre-commit 配置/pyproject/uv.lock/.venv/settings/logging/healthz + 7 个测试全绿、ruff 零告警），本轮接续 T6-T13。
- **重大纠正：schema.sql 实为 18 张表，不是开发计划/三件套里写的"15 表"**（用户权限 3 + 知识库 6 + Skill/Agent 4 + 会话运行 4 + 审计 1）。已批量修订 spec/tasks/checklist/开发计划四份文档。
- T6 四件套全 healthy；**docker.io/minio/minio 被国内镜像站（daocloud）403 → compose 改用 quay.io/minio/minio + quay.io/minio/mc**。pgvector 扩展可用、minio-init 自动建桶 yunzhi-files 成功。
- T7 模型 18 表（base.py 公共基类 + user/kb/agent/session/audit 五文件），T8 TDD：先写 test_migration.py（临时库 yunzhi_migration_test + alembic Python API + information_schema 断言）→ autogenerate → **人工校对抓到 3 类 autogenerate 错误**（这就是 spec 要求校对的意义）：① JSONB/TEXT[] 默认值 `'{}'::jsonb` 引号被吃（要手工改成 sa.text()）② file_size/audit.user_id 应为 BigInteger ③ 5 个可空时间列缺 timezone=True。修正后 10 测试全绿、开发库迁移完成。
- T5 补 TDD 循环：加 test_main.py 启动日志测试（Red）→ main.py 加 structlog server.started（Green）。
- **Windows 中文环境三连坑**（已写进 README）：① alembic 按 locale(GBK) 读 alembic.ini，中文注释 UnicodeDecodeError → ini 必须纯 ASCII ② psycopg 连 localhost 挂死（IPv6 回退），改 127.0.0.1 秒连 ③ 测试 teardown 删库报 ObjectInUse → inspect engine 必须 dispose + DROP DATABASE WITH (FORCE)。
- **沙箱环境坑**：Git Bash 无 ls/grep/tail/find/sed（用 Glob/PowerShell/Node 脚本替代）；PowerShell 工具输出被吞但命令可执行；corepack/npm 的 shell 包装器坏了，须 `node.exe corepack.js` / `node.exe npm-cli.js` 直调；registry.npmjs.org 被代理 502，走 `COREPACK_NPM_REGISTRY=https://registry.npmmirror.com`；pnpm store 默认落 E:\.pnpm-store 被沙箱拒绝访问 → 项目内 store-dir；**pnpm 建 symlink 被沙箱彻底拦截（node-linker=hoisted 也绕不过）→ 沙箱内只能 npm install，pnpm 仍是用户本机的规范工具**。
